{% extends 'base.html.twig' %}

{% block title %}LibContact{% endblock %}

{% block body %}
    <h1>LibContact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lib_contact.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ lib_contact.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_utilisateurs_lib_lib_contact_index') }}">back to list</a>

    <a href="{{ path('app_utilisateurs_lib_lib_contact_edit', {'id': lib_contact.id}) }}">edit</a>

    {{ include('utilisateurs/lib/lib_contact/_delete_form.html.twig') }}
{% endblock %}
