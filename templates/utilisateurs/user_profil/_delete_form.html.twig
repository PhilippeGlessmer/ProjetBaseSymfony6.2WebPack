<form method="post" action="{{ path('app_utilisateurs_user_profil_delete', {'id': user_profil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_profil.id) }}">
    <button class="btn">Delete</button>
</form>
